<!DOCTYPE html>
<html>
<head>
  <title>Foodspotting Clone</title>
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for?(:title) ? yield(:title) : "Rails Bootstrap" %></title>
  <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Rails Bootstrap" %>">
<!--   <link rel="stylesheet" href="http://octodex.github.com/css/screen.css" media="screen">
  <link rel="stylesheet" href="http://octodex.github.com/css/core.css" media="screen"> -->
  <%= stylesheet_link_tag "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body class="index">
	<% flash.each do |key, value| %>
    <%= content_tag(:div, value) %>
  <% end %>
  <div class="navbar navbar-fixed-top navbar-inverse" role="navigation">
    <div class="container">
      <div class = "navbar-header">
          <a class="navbar-brand" href="#">dish</a>
      </div>

      <div class="collapse navbar-collapse">
        <ul class="nav navbar-nav">
          <li class="active"><%= link_to "Submit a dish!", new_dish_path %></li>
          <li><a href="#about">About</a></li>
          <li><a href="#contact">Contact</a></li>
        </ul>
      </div>
    </div>
  </div>
  <div class="content">
    <div class="row row-offcanvas row-offcanvas-right">
      <div class="col-xs-12 col-sm-9">
        <div class="row">
          <%= yield %>
        </div>
      </div>
    </div>
  </div>
  <%= render 'layouts/footer' %>
</body>
</html>